extends layout.pug

block content 
    div.container 
        h1 All Dreams 

        each note in notes 
            div.note-wrapper
                a.note(href='/notes/$ { notes.id }') #{ note.title }
                a.delete(href='#') Delete